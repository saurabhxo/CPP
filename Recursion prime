#include <iostream>
using namespace std;
int prime(int n,int i);
int count=0;
int main() 
{
    int n,c;
    cin>>n;
    c=prime(n,1);
    if(c==1)
    cout<<"prime";
    else
    cout<<"Not";
	return 0;
}
int prime(int n,int i)
{
    if(i==n)
    return count;
    else 
    {
    if(n%i==0)
    count++;
    i++;
    prime(n,i);
    }
}
************************************************************************************
Sample Input
5
Sample Output
Prime
